<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DeutschlandApp">
        <meta-data

            android:name="com.maptiler.simplemap.mapTilerKey"
            android:value="IEgjtP6buJHzlPpbjWrk" />

        <activity
            android:name=".ui.mailbox.MailBoxActivity"
            android:exported="true"
            android:label="@string/title_activity_mail_box"
            android:theme="@style/Theme.DeutschlandApp.NoActionBar"></activity>

        <activity
            android:name=".ui.geo.GeoDataActivity"
            android:exported="true"
            android:label="@string/title_activity_geo_data"></activity>
        <activity
            android:name=".ui.account.login.AccountLoginActivity"
            android:exported="true"
            android:label="@string/title_activity_register_account"></activity>
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DeutschlandApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>